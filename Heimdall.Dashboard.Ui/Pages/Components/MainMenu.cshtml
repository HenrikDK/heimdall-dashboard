<template id="main-menu">
    <div class="bg-neutral-200">
        <nav class="fixed">
            <div class="group min-w-12 min-h-12">
                <router-link to="/workloads" class="flex flex-col items-center pointer py-3 !text-black">
                    <deployments-btn :active="$route.path.startsWith('/workloads')"></deployments-btn>
                </router-link>
            </div>

            <div class="group min-w-12 min-h-12">
                <router-link to="/pods" class="flex flex-col items-center pointer py-3 !text-black">
                    <pods-btn :active="$route.path.startsWith('/pods')"></pods-btn>
                </router-link>
            </div>

            <div class="group min-w-12 min-h-12">
                <router-link to="/services" class="flex flex-col items-center pointer py-3 !text-black">
                    <services-btn :active="$route.path.startsWith('/services')"></services-btn>
                </router-link>
            </div>

            <div class="group min-w-12 min-h-12">
                <router-link to="/ingresses" class="flex flex-col items-center pointer py-3 !text-black">
                    <img v-if="$route.path.startsWith('/ingresses')" src="art/ing-blue.svg" class="w-10 h-10 group-hover:scale-125" alt="Ingress"/>
                    <img v-else src="art/ing-grey.svg" class="w-10 h-10 group-hover:scale-125" alt="Ingress"/>
                    <span class="text-xs m-1 uppercase">Ingress</span>
                </router-link>
            </div>
        </nav>
    </div>
</template>

<script>
    components["main-menu"] = {
      template: "#main-menu",
    };
</script>