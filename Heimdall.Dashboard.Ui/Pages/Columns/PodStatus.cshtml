<template id="pod-status">
  <div class="px-4 pt-2 flex py-0.5">
    <template v-for="item in getContainerStatuses(row)">
      <div class="mr-2">
      </div>
    </template>
  </div>
</template>

<script type="module">
let setup = (props, ctx) => {
  
  return { }
}

components["pod-status"] = { template: "#pod-status", setup, 
  props: {
    row: {}
  }
};
</script>
